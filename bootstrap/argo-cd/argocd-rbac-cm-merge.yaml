apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-rbac-cm
data:
  policy.default: role:''
  policy.csv: |
    # prd
    p, role:g_argocd_charlie_admin, applications, sync, */*, allow
    p, alex_abreu, role:g_argocd_charlie_admin
